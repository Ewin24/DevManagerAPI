# Build output
bin/
obj/
packages/

# IDE
.vs/
*.suo
*.user
*.userosscache
*.sln.docstates
*.swp

# Secrets — NEVER commit real credentials
appsettings.*.json
appsettings.json
*.env
.env.local

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Temp
nul

# Binary artifacts
*.exe
*.zip
*.rar
*.pdf

# Local working files — NEVER commit
# .atl/ holds the skill registry cache, regenerated by `gentle-ai skill-registry refresh`
# openspec/ holds SDD working artifacts; the source of truth lives in Engram memory
# Both are regenerated from canonical sources, no need to track them
.atl/
openspec/
